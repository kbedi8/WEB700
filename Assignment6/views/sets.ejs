<!DOCTYPE html>
<html>

<head>
    <title>Lego Collection - Sets</title>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/theme.css">
</head>

<body>
    <%- include('partials/navbar', { page: '/lego/sets' }) %>

    <div class="container">
        <div class="row">
            <main class="col-md-12">
                <section class="bg-light text-center py-5">
                    <div class="container">
                        <h1 class="display-5 fw-bold">Collection</h1>
                        <p class="lead text-muted">
                            Browse all Lego sets currently in the collection.
                        </p>
                        <div class="mt-3">
                            <span class="fw-semibold me-2">Quick filter:</span>
                            <a href="/lego/sets?theme=technic" class="btn btn-outline-primary btn-sm me-1">Technic</a>
                            <a href="/lego/sets?theme=classic" class="btn btn-outline-primary btn-sm me-1">Classic</a>
                            <a href="/lego/sets?theme=town" class="btn btn-outline-primary btn-sm me-1">Town</a>
                        </div>
                    </div>
                </section>

                <section class="py-4">
                    <div class="container">
                        <% if (sets && sets.length > 0) { %>
                            <div class="table-responsive">
                                <table class="table table-striped align-middle">
                                    <thead>
                                        <tr>
                                            <th scope="col">Image</th>
                                            <th scope="col">Set</th>
                                            <th scope="col">Theme</th>
                                            <th scope="col">Year</th>
                                            <th scope="col">Parts</th>
                                            <th scope="col">Actions</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        <% sets.forEach(function(set) { %>
                                            <tr>
                                                <td style="width: 120px;">
                                                    <% if (set.img_url) { %>
                                                        <img src="<%= set.img_url %>" alt="<%= set.Theme.name %>" class="img-thumbnail">
                                                    <% } else { %>
                                                        <span class="text-muted small">N/A</span>
                                                    <% } %>
                                                </td>
                                                <td>
                                                    <a href="/lego/sets/<%= set.set_num %>"><%= set.Theme.name %></a>
                                                    <div class="text-muted small">
                                                        Set #: <%= set.set_num %>
                                                    </div>
                                                </td>
                                                <td>
                                                    <span class="badge bg-secondary"><%= set.Theme.name %></span>
                                                </td>
                                                <td><%= set.year %></td>
                                                <td><%= set.num_parts %></td>
                                                <td>
                                                    <a href="/lego/sets/<%= set.set_num %>"
                                                        class="btn btn-sm btn-primary">Details</a>
                                                    <a href="/lego/deleteSet/<%= set.set_num %>"
                                                        class="btn btn-sm btn-outline-danger ms-1">Delete</a>
                                                </td>
                                            </tr>
                                        <% }) %>
                                    </tbody>
                                </table>
                            </div>
                        <% } else { %>
                            <p class="text-muted text-center">No sets found.</p>
                        <% } %>
                    </div>
                </section>
            </main>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
        crossorigin="anonymous"></script>
</body>

</html>
